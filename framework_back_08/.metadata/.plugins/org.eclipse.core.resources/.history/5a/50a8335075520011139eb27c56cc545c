package com.ssafy.ws.model.dao;

import java.util.List;

import org.apache.ibatis.annotations.Mapper;

import com.ssafy.ws.model.dto.Book;

/**
 * 8-2: 등록 - BookDao 인터페이스
 *
 * 실습 과제: INSERT 문을 이용한 등록 기능 구현
 * (SELECT + INSERT = CR)
 */
@Mapper
public interface BookDao {

    // ============ READ (8-1에서 학습) ============
    /**
     * 모든 책 목록 조회
     */
    List<Book> selectAll();

    /**
     * ISBN으로 책 조회
     */
    Book select(String isbn);

    // ============ CREATE (8-2 실습) ============
    // TODO: 책 등록 메서드를 선언하세요.
    // - 메서드명: insert
    // - 파라미터: Book book
    // - 반환값: int (등록된 행 수)
    
}
