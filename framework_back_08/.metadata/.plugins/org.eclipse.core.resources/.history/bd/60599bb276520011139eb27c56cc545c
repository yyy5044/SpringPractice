<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!--
    8-2: 등록 실습

    학습 포인트:
    1. INSERT 문 작성법
    2. parameterType: 전달받는 객체 타입
    3. #{필드명}: 객체의 getter를 통해 값 바인딩
-->
<mapper namespace="com.ssafy.ws.model.dao.BookDao">

    <!-- READ: 목록 조회 (8-1) -->
    <select id="selectAll" resultType="Book">
        SELECT isbn, title, author, price
        FROM book
    </select>

    <select id="select" parameterType="String" resultType="Book">
        SELECT isbn, title, author, price
        FROM book
        WHERE isbn = #{isbn}
    </select>

    <!-- CREATE: 등록 (8-2 실습) -->
    <!--
        TODO: INSERT 문을 작성하세요.
        - id: insert
        - parameterType: Book
        - #{isbn}, #{title}, #{author}, #{price}를 사용하여 값을 바인딩합니다.
    -->
	<insert id="insert"></insert>
</mapper>
